/**
 * Production environment. This is the default file; other configurations
 * replace it via `fileReplacements` in angular.json.
 */
export const environment = {
  production: true,
  envName: 'production',
  /** Base URL for the future backend API (empty = same origin, proxied by the host). */
  apiBaseUrl: '/api',
};
