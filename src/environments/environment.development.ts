/**
 * Development environment — used by `ng serve` and development builds.
 */
export const environment = {
  production: false,
  envName: 'development',
  /** Base URL for the future backend API during local development. */
  apiBaseUrl: 'http://localhost:3000/api',
};
